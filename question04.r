# Criando dois vetores
vetor1 <- c(1, 2, 3, 5)
vetor2 <- c(4, 5)
#os vetores precisam ser mutiplos para as Operações Aritméticas funcionarem
# Soma dos vetores
resultado <- vetor1 + vetor2
print(resultado)